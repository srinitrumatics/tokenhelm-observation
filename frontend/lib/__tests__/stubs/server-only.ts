// Test stub for the `server-only` package. In Next.js this module guards against
// importing server code into client bundles; under Vitest we replace it with a no-op
// so the pure file-reading logic can be unit-tested directly.
export {};
